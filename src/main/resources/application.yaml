spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/database
    username: root
    password: 123456789!
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  activemq:
    pool:
      max-connections: 10
server:
  port: 8444
